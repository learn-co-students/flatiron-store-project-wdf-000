<h1><%= @category.title %></h1>

<ul>
<% @category.items.each do |item| %>
    <li><%= link_to item.title+" "+number_to_currency(item.price/100), item_path(item) %><%= button_to 'Add to Cart', line_items_path(item_id: item), class: 'btn btn-primary' if user_signed_in? %></li>
<% end %>
</ul>